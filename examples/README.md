# examples

Scratch space for example and testing files: exported timelines, sample photos,
hand-made JSON. Everything here except this README is ignored by git (see .gitignore).

Schema fixtures that should be committed belong in tests/fixtures/ instead.
